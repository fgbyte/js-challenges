// Calculate Power of a Number
// Write a function 'calculatePower' in Typescript that takes two input parameters - base and exponent. The function should return the result of the base raised to the power of exponent. The function should correctly handle positive, zero and negative exponent values. For negative exponent, return the reciprocal of the base's absolute power to the exponent.

// Examples
// calculatePower(2,3)	8
// calculatePower(5, 0)	1
// calculatePower(2, -3)	0.125

function calculatePower(base: number, exponent: number): number {
  // const a = 1;
  // return a;
}

console.log(calculatePower(2, 2));
